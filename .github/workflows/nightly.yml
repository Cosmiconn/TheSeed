name: Nightly Tests

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

env:
  VCPKG_ROOT: ${{ github.workspace }}/vcpkg

jobs:
  nightly-linux:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          token: ${{ secrets.SUBMODULE_TOKEN }}

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build g++-13 clang-tidy cppcheck valgrind lcov

      - name: Clone vcpkg
        run: |
          if [ ! -d "$VCPKG_ROOT/.git" ]; then
            rm -rf "$VCPKG_ROOT"
            git clone https://github.com/Microsoft/vcpkg.git "$VCPKG_ROOT"
          fi

      - name: Bootstrap vcpkg
        run: |
          cd "$VCPKG_ROOT"
          if [ ! -f "./vcpkg" ]; then
            ./bootstrap-vcpkg.sh
          fi

      - name: Cache vcpkg
        uses: actions/cache@v4
        with:
          path: |
            ${{ github.workspace }}/vcpkg/installed
            ${{ github.workspace }}/vcpkg/downloads
          key: vcpkg-linux-${{ hashFiles('vcpkg.json') }}

      - name: vcpkg install
        run: |
          "$VCPKG_ROOT/vcpkg" install --triplet=x64-linux

      - name: Configure Debug with Sanitizers
        run: |
          cmake -S . -B build/linux-debug \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_CXX_COMPILER=g++-13 \
            -DCMAKE_TOOLCHAIN_FILE="$VCPKG_ROOT/scripts/buildsystems/vcpkg.cmake" \
            -DVCPKG_TARGET_TRIPLET=x64-linux \
            -DSEED_ENABLE_SANITIZERS=ON \
            -DBUILD_TESTING=ON

      - name: Build Debug
        run: cmake --build build/linux-debug --parallel

      - name: Run all tests (except performance gates)
        run: ctest --test-dir build/linux-debug --output-on-failure -E seed_gate_perf_tests
        env:
          ASAN_OPTIONS: detect_leaks=1:abort_on_error=1:print_stats=1
          UBSAN_OPTIONS: print_stacktrace=1:halt_on_error=1

      - name: Stress Tests x20 (except performance gates)
        run: |
          export ASAN_OPTIONS=detect_leaks=1:abort_on_error=1
          export UBSAN_OPTIONS=print_stacktrace=1:halt_on_error=1
          for i in $(seq 1 20); do
            echo "=== Stress run $i ==="
            ctest --test-dir build/linux-debug --output-on-failure -E seed_gate_perf_tests
          done

      # Valgrind requires a build WITHOUT sanitizers (ASan replaces malloc/free).
      - name: Configure Debug (no sanitizers) for Valgrind
        run: |
          cmake -S . -B build/linux-debug-valgrind \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_CXX_COMPILER=g++-13 \
            -DCMAKE_TOOLCHAIN_FILE="$VCPKG_ROOT/scripts/buildsystems/vcpkg.cmake" \
            -DVCPKG_TARGET_TRIPLET=x64-linux \
            -DSEED_ENABLE_SANITIZERS=OFF \
            -DBUILD_TESTING=ON

      - name: Build Debug (no sanitizers) for Valgrind
        run: cmake --build build/linux-debug-valgrind --parallel

      - name: Valgrind Memory Check – All Tests
        run: |
          # Run Valgrind on every test binary. Each binary is invoked once;
          # doctest runs all test cases inside it automatically.
          # NOTE: Performance tests are excluded because Valgrind slows code
          # by 10-50x, making timing thresholds meaningless.
          FAILED=0
          for binary in \
            "build/linux-debug-valgrind/submodules/seed-core/tests/seed_tests" \
            "build/linux-debug-valgrind/submodules/seed-core/tests/seed_property_tests" \
            "build/linux-debug-valgrind/submodules/seed-core/tests/seed_fuzz_pool" \
            "build/linux-debug-valgrind/submodules/seed-network/tests/seed_network_tests" \
            "build/linux-debug-valgrind/submodules/seed-network/tests/seed_network_integration_tests" \
            "build/linux-debug-valgrind/submodules/seed-network/tests/seed_network_property_tests" \
            "build/linux-debug-valgrind/submodules/seed-network/tests/seed_network_fuzz_transport" \
            "build/linux-debug-valgrind/tests/integration/seed_integration_tests" \
            "build/linux-debug-valgrind/tests/e2e/seed_e2e_tests" \
            "build/linux-debug-valgrind/tests/gate/seed_gate_tests" \
            "build/linux-debug-valgrind/tests/fuzz/seed_meta_fuzz_arena" \
            "build/linux-debug-valgrind/tests/fuzz/seed_meta_fuzz_serialize"
          do
            if [ ! -f "$binary" ]; then
              echo "SKIP: $binary not found"
              continue
            fi
            name=$(basename "$binary")
            echo "=== Valgrind: $name ==="
            # Exclude performance/timing tests; keep functional/memory tests
            valgrind --leak-check=full --show-leak-kinds=all --track-origins=yes \
              --error-exitcode=42 "$binary" -tc="!*Performance*" -tc="!*speed*" -tc="!*Throughput*" -tc="!*Stress*" || {
              echo "ERROR: Valgrind detected issues in $name"
              FAILED=1
            }
          done
          if [ "$FAILED" -ne 0 ]; then
            echo "Valgrind found memory errors. Failing build."
            exit 1
          fi

      - name: Configure Coverage
        run: |
          cmake -S . -B build/linux-coverage \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_CXX_COMPILER=g++-13 \
            -DCMAKE_TOOLCHAIN_FILE="$VCPKG_ROOT/scripts/buildsystems/vcpkg.cmake" \
            -DVCPKG_TARGET_TRIPLET=x64-linux \
            -DSEED_ENABLE_COVERAGE=ON \
            -DSEED_ENABLE_SANITIZERS=OFF \
            -DBUILD_TESTING=ON

      - name: Build Coverage
        run: cmake --build build/linux-coverage --parallel

      - name: Generate Coverage Report
        run: |
          cd build/linux-coverage
          cmake --build . --target coverage-report

      - name: Upload Coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: build/linux-coverage/coverage/coverage.info.filtered
          fail_ci_if_error: true
          verbose: true

      - name: Static Analysis – clang-tidy
        run: |
          # Configure a dedicated Clang build for clang-tidy to avoid GCC-only
          # warning flags (-Wduplicated-branches, -Wlogical-op, etc.) that
          # clang-tidy-18 treats as hard errors when reading compile_commands.json
          # from a GCC build.
          cmake -S . -B build/clang-tidy \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCMAKE_C_COMPILER=clang \
            -DCMAKE_CXX_COMPILER=clang++ \
            -DCMAKE_TOOLCHAIN_FILE="$VCPKG_ROOT/scripts/buildsystems/vcpkg.cmake" \
            -DVCPKG_TARGET_TRIPLET=x64-linux \
            -DBUILD_TESTING=ON \
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
          run-clang-tidy -p build/clang-tidy submodules/seed-core/src/ submodules/seed-core/tests/ submodules/seed-network/src/ submodules/seed-network/tests/

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: TheSeed-Nightly-Linux
          path: build/linux-debug/submodules/seed-core/tests/seed_tests
